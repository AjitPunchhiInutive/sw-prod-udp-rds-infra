name: Terragrunt Action Workflow Prod

on:
 workflow_dispatch:
 push:
  branches:
  - main

jobs:
 terragrunt-job:
    #uses: ../gh-actions-library/.github/workflows/terragrunt-gcp-prod.yml
     uses: AjitPunchhiInutive/sw-prod-udp-rds-infra/blob/main/.github/workflows/terragrunt-folders-workflow.yml
    with:
      workingDirectory: folders
      environment: prod
      tfLog: 'ERROR'